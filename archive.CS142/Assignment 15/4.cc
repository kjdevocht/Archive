/*Kevin DeVocht assignment 15 exercise4. Develop the function truncate which 
consumes a parent and a generation number and produces a parent that does not 
have any descendents in that generation (and thus, obviously, subsequent generations.)
*/

#include <stdio.h>
#include <math.h>
#include <string.h>